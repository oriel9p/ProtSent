# Late interaction over ProtSent — results

Generated by `build_late_results.py` from the result CSVs. Do not hand-edit.

## Method

Late interaction keeps one embedding per residue and scores a pair by **MaxSim** (for each
query residue, its best match in the document, summed) instead of comparing two mean-pooled
vectors by cosine. Every trained arm exports both views from the *same weights* — a `late/`
multi-vector model and a `dense_view/` mean-pooled model — so the MaxSim-vs-cosine comparison
is a pure scoring difference with the weights held fixed.

Scores are **mean** MaxSim (divided by query length). Raw MaxSim sums over query residues and
so scales with length: harmless where variants share a length, a pure artifact where they do
not (indels).

Arms named `*_zeroshot` score MaxSim over the backbone's **native residues with no projection**
and no late training. They separate two things that are easy to confuse: whether MaxSim is a
better *scorer*, and whether late *training* helps.

## SCOPe-40 structural retrieval

All-vs-all over 2,207 domains at three SCCS levels. Metrics are eligible-query means (queries
with at least one non-self same-label neighbour); MAP is over the full ranking.

| arm | scoring | fold MAP | superfamily MAP | family MAP |
|---|---|---|---|---|
| `esm2_dense` | pooled cosine | 0.3582 | 0.3981 | 0.4214 |
| `esm2_zeroshot` | MaxSim | 0.4043 | 0.4735 | 0.5454 |
| `esm2_late` | MaxSim | 0.6000 | 0.6705 | 0.6980 |
| `protsent_v2_dense` | pooled cosine | 0.5820 | 0.6540 | 0.6461 |
| `protsent_v2_zeroshot` | MaxSim | 0.6014 | 0.6831 | 0.6841 |
| `protsent_late` | MaxSim | 0.6019 | 0.6721 | 0.6805 |
| `protsent_late_480` | MaxSim | 0.6247 | 0.6984 | 0.6949 |
| `esm2_late_480` | MaxSim | 0.5117 | 0.5939 | 0.6751 |
| `protsent_late_proj128` | MaxSim | 0.6288 | 0.7057 | 0.7087 |
| `protsent_v2_150m_dense` | pooled cosine | 0.5917 | 0.6702 | 0.7045 |
| `protsent_v2_150m_zeroshot` | MaxSim | 0.6303 | 0.7192 | 0.7496 |
| `protsent_late_35m_prop` | MaxSim | 0.6088 | 0.6896 | 0.7065 |
| `protsent_late_35m_prop_dense` | pooled cosine | 0.5873 | 0.6533 | 0.6477 |
| `protsent_late_150m_prop` | MaxSim | 0.6384 | 0.7220 | 0.7571 |
| `protsent_late_150m_prop_dense` | pooled cosine | 0.6037 | 0.6735 | 0.7027 |
| `protsent_late_proj128_dense` | pooled cosine | 0.6023 | 0.6664 | 0.6522 |
| `protsent_late_150m` | MaxSim | 0.6600 | 0.7376 | 0.7607 |
| `protsent_late_150m_dense` | pooled cosine | 0.6263 | 0.6864 | 0.7022 |
| `protsent_late_150m_prop@25000` | MaxSim | 0.6383 | 0.7222 | 0.7567 |

Arms suffixed `_bf16bug` and `protsent_late_capped_flash` are **deleted**, models and rows
alike. They trained under a bug that put AdamW's parameters in bf16, where a 1e-5 update is
below the representable spacing, so only 2.4% of backbone elements could move. The analysis
survives in RUNS.md; the artifacts do not, and `build_late_results.py` still filters the names
so a CSV restored from git cannot reintroduce them.

## ProteinGym — not available

> The only ProteinGym data for these arms was a partial run (500-variant cap, 512-residue
> truncation); it has been deleted, so there is nothing to report here. Rerun at full
> coverage (~1.0 h/arm) to populate this section.

The second paired row is **not a step-count contrast**, despite once being labelled as one.
`protsent_late_35m_prop` continues from `protsent_late_proj128`, but changes five things at
once: sampler (round-robin → proportional), pool (2.0M/2.0M → 19.0M AFDB / 15.0M STRING),
world size (1 → 2, so effective batch 128 → 256), attention (sdpa → vllm-flash-attn3) and
`--compile`. Both `runtime.json` files record this. Decisively, the 15.5x extra pairs bought
**1.04x** the Pfam exposure (170,667 → 177,441 pairs): round-robin gave Pfam a third of every
batch, proportional gives it 2.24%. The extra training is ~all AFDB and STRING. Read the row
as a cost of the phase-2 mixture, not evidence that training longer hurts.

For the clinical variants the score is **negated** before the AUC: the label is pathogenicity,
and a variant that looks more like the wild type should be less pathogenic.

### dms_indels — spearman, 66 groups

| arm | scoring | spearman | 95% CI |
|---|---|---|---|
| `proj128_late` | MaxSim | 0.5214 | [0.4843, 0.5587] |
| `protsent_late_35m_prop_late` | MaxSim | 0.5192 | [0.4820, 0.5568] |
| `protsent_late_35m_prop_dense` | pooled cosine | 0.4956 | [0.4564, 0.5336] |
| `protsent_v2_dense` | pooled cosine | 0.4904 | [0.4513, 0.5307] |

| paired comparison | Δ | 95% CI |
|---|---|---|
| MaxSim − pooled cosine (identical weights) (n=66) | +0.0236 ** | [+0.0079, +0.0395] |
| phase-2 proportional recipe − phase-1 round-robin arm (5 confounds, NOT step count) (n=66) | -0.0022 | [-0.0089, +0.0047] |

### clinical_indels — auc, 77 groups

| arm | scoring | auc | 95% CI |
|---|---|---|---|
| `proj128_late` | MaxSim | 0.8545 | [0.7824, 0.9135] |
| `protsent_late_35m_prop_late` | MaxSim | 0.8488 | [0.7825, 0.9107] |
| `protsent_v2_dense` | pooled cosine | 0.7015 | [0.6169, 0.7892] |
| `protsent_late_35m_prop_dense` | pooled cosine | 0.7003 | [0.6146, 0.7870] |

| paired comparison | Δ | 95% CI |
|---|---|---|
| MaxSim − pooled cosine (identical weights) (n=77) | +0.1485 ** | [+0.0724, +0.2279] |
| phase-2 proportional recipe − phase-1 round-robin arm (5 confounds, NOT step count) (n=77) | -0.0057 | [-0.0433, +0.0329] |

